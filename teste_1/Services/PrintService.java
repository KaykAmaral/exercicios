/*
package exercicios.teste_1.Services;

import java.util.ArrayList;

public class PrintService<T> {
    // The 'T' is a generic type, it can be String, Double, int, Integer... Whatever...

    private ArrayList<T> list = new ArrayList<>();

    public void addItem(T value) { list.add(value); }

    public T first() {
        if (list.isEmpty()) { throw new IllegalStateException("[ERROR] - The list is Empty"); }
        return list.getFirst();
    }

}
*/